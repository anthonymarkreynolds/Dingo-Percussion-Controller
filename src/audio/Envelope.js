class Envelope {
  contructor(attack, decay, sustain, release, length = 1, amount = 0.5) {
    this.attack = attack;
    this.decay = decay;
    this.sustain = sustain;
    this.release = release;
    this.length = length;
    this.amount = amount;
  }
}

class Pad {
  constructor(actx, 
}
